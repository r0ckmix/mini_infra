apiVersion: v1
kind: Service
metadata:
  name: dashboard-proxy-svc
  namespace: kubernetes-dashboard
spec:
  ports:
    - name: dashboard-proxy-tls
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: dashboard-proxy-deployment
  type: LoadBalancer